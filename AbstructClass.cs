using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Giris
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             * 1) Abstract classlar özet class olarak bilinirler
             * 2) Hiyerarşinin tepesinde nesnlerin yetersiz olduğu durumlarda kullanılır.
             * 3) Abstract classlar Abstract ifadesiyle belirtilirler.
             * 4) Abstract classlar içerisinde alan tanımlaması yapılabilir
             * 5) Abstract classlar içersinde Propertiesler get ve set blokları olamadan yazılablir.
             * 6) Abstract classlar içerisinde gövdesi olmayan mettolar yazılacaksa
             *    ilgili metotlar da Abstract kelimesiyle bildirilmelidir.
             * 7) Abstract metotlar başka classlar tarafından kullanılacağı için
             *    erişim belirteçleri public olmalıdır.
             * 8) Abstract metotlar başka claslarda kalıtım yolu ile kullanılcaksa 
             *    ilgili class içerisinde implement edilmelir. Implement edilmiş 
             *    metotların kullanılabilmesi için override edilmeli.
             * 9) Abstract classlar içerisnde gövdesi olan normal metotlar 
             *    yazılablir. Bu metotlar istek üzerine başka classlara miras 
             *    verilebilir.
             * 10)Abstract classlar içerinde constructorlar yazılabilir.
             * 
             *    
             */
        }
    }
}
